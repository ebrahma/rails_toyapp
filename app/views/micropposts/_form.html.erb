<%= form_for(@microppost) do |f| %>
  <% if @microppost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@microppost.errors.count, "error") %> prohibited this microppost from being saved:</h2>

      <ul>
      <% @microppost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
